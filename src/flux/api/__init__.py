"""Flux web API (FastAPI + SQLAlchemy 2.x async).

Exposes the agent runtime as an HTTP service so a Next.js frontend (or any
other client) can authenticate via GitHub OAuth and manage agents over the
network. CLI usage continues to work unchanged.
"""
